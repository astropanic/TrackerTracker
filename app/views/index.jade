extends layout

block content

  #header
    #logo
      h1 <strong>Tracker</strong>Tracker

      #account
        a.sign-out(href='/settings') Settings
        a.sign-out(href='/signOut') Sign Out

    #nav
      #projects
      #filters
      #tags

    #search
      input(type='search')

  #content
    #columns

  #footer
